"""labcore — núcleo compartilhado do laboratório CredSim.

Reutilizado pelo backend (FastAPI) e pelos notebooks das aulas. Contém o cliente
de LLM (mock/real), os cenários, as defesas (toggles) e o logging.
"""
